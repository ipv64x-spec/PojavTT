# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties (Minecraft 1.21.3)
# BEST-EFFORT values - verify against https://fabricmc.net/develop if Loom
# reports a missing/invalid yarn_mappings, loader_version, or fabric_version.
# Loom errors list the valid options directly when one of these is wrong.
minecraft_version=1.21.3
yarn_mappings=1.21.3+build.2
loader_version=0.16.10
loom_version=1.9-SNAPSHOT

# Mod Properties
mod_version=1.21.3
maven_group=com.pojavtiers
archives_base_name=pojavtiertagger

# Dependencies
# NOTE: 0.110.5+1.21.3 was CONFIRMED NOT TO EXIST by a real CI run - this is
# a corrected best-effort guess, still unverified (no network access here to
# check https://fabricmc.net/develop directly). If this specific number is
# still wrong, ONLY this :1.21.3 build will fail (see root gradle.properties
# for why other versions are now unaffected) - check the Loom error, which
# lists real available versions, and update just this one line.
fabric_version=0.110.0+1.21.3
modmenu_version=13.0.0
