# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties (Minecraft 1.21.7)
# BEST-EFFORT values - verify against https://fabricmc.net/develop if Loom
# reports a missing/invalid yarn_mappings, loader_version, or fabric_version.
# Loom errors list the valid options directly when one of these is wrong.
minecraft_version=1.21.7
yarn_mappings=1.21.7+build.1
loader_version=0.18.4
loom_version=1.12-SNAPSHOT

# Mod Properties
mod_version=1.21.7
maven_group=com.pojavtiers
archives_base_name=pojavtiertagger

# Dependencies
# NOTE: 0.130.0+1.21.7 was CONFIRMED NOT TO EXIST by a real CI run. Adjusted
# guess below, interpolated from CONFIRMED real values either side of this
# version (1.21.6 -> 0.128.0+1.21.6, 1.21.8 -> 0.132.0+1.21.8, both confirmed
# by successfully passing dependency resolution in CI) - still unverified.
# If wrong, ONLY this :1.21.7 build fails; the Loom error will list the real
# available version to paste back.
fabric_version=0.129.0+1.21.7
modmenu_version=15.0.0
