# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties (Minecraft 1.21.9)
# BEST-EFFORT values - verify against https://fabricmc.net/develop if Loom
# reports a missing/invalid yarn_mappings, loader_version, or fabric_version.
# Loom errors list the valid options directly when one of these is wrong.
minecraft_version=1.21.9
yarn_mappings=1.21.9+build.1
loader_version=0.18.4
loom_version=1.13-SNAPSHOT

# Mod Properties
mod_version=1.21.9
maven_group=com.pojavtiers
archives_base_name=pojavtiertagger

# Dependencies
# NOTE: 0.135.0+1.21.9 was CONFIRMED NOT TO EXIST by a real CI run. Adjusted
# guess below, interpolated from CONFIRMED real values either side of this
# version (1.21.8 -> 0.132.0+1.21.8, 1.21.10 -> 0.138.0+1.21.10, both
# confirmed working in CI) - still unverified. If wrong, ONLY this :1.21.9
# build fails; the Loom error will list the real available version to paste back.
fabric_version=0.134.0+1.21.9
modmenu_version=16.0.0
